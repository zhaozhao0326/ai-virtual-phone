// lib/mixology/crafting-guides.ts
// 独家特调 · 各类材料的制作说明（委托词）。
//
// 每段都是一份写好的"发给任意 AI 的委托"：把这一类材料的应用机制、格式约束、
// 输出分段一次性交代清楚，作者只需在末尾【我的想法】里补一句自己的点子，
// 整段复制发给 AI，就能拿到可以逐框贴回编辑器的内容。
// 默认让 AI 按编辑器的框逐段输出（贴回来最稳）；每段末尾都附了改要 JSON 的口令，
// 想一步到位的作者可以让 AI 直接产出能从酒柜导入的 .json 文件内容。
//
// 覆盖必须与编辑器的框一一对应（含所有种类共有的名称/一句话介绍/标签），
// 机制描述必须与真实实现一致（壳标记、注入变量、mix.* API、各项上限、
// 开场白的 --- 分隔）。改了编辑器或协议记得回来改这份文案——
// 委托词撒谎或缺框，AI 产出的东西就贴不进来。

import type { MixMaterialKind } from "./types";

export const MIX_CRAFT_PROMPTS: Record<MixMaterialKind, string> = {
    character: `请帮我创作一张 AI 角色扮演用的角色卡。写作时可用两个占位符：{{char}} 代表角色自己，{{user}} 代表玩家；需要小标题时用 ### 开头（# 和 ## 已被应用占用）。

请按下面的框逐段输出，段与段之间用【框名】隔开（没有内容的框可省略）：
【角色名】角色叫什么
【一句话介绍】给读者看的一行钩子文案
【标签】3~8 个短词，用顿号隔开，例：现代都市、暗恋、久别重逢
【基础信息】年龄/身高/职业等，键值行
【性格】具体的性格，带行为例证
【外貌】
【背景】成长经历与关键往事
【世界观】所处世界的公共设定
【初始认知】开局时{{char}}对{{user}}知道什么、是什么印象
【关系与身份】推荐{{user}}可代入的身份，以及各身份下的关系
【当前剧情】开局那一刻的情境
【附加设定】NPC、专有名词表等自由补充
【开场白】2~3 条供玩家挑选；条与条之间用单独一行 --- 分隔（编辑器按这个分隔符拆开）
【示例对话】3~5 轮，玩家与角色交替；每轮单独一行，以「user：」或「char：」开头，用来锚定文风
【开场画布】按最下方的「画布制作规格」输出一份完整 HTML

▍写作要求（每一框都照此执行）：
- 用具体的行为与细节说话，不堆形容词：「工作人员叫错他的餐，他通常自己吃掉」胜过「温和隐忍」；
- 一句话介绍要有时间差或转折的张力，让人想点开；
- 世界观写这个世界实际如何运转的规则与代价，不写口号；
- 初始认知写清楚{{char}}知道{{user}}什么、不知道什么——边界感就是代入感；
- 关系与身份给 2~3 个不同的可代入入口，各配一句该入口下的相处基调；
- 附加设定里的配角各有立场与来路、与主角的关系里带一点说不清对错的灰度，不当推剧情的工具人；
- 开场白低开：从一个具体的时刻与动作进入，情绪收着写，结尾停在给{{user}}接话的地方，绝不替{{user}}行动或说话；篇幅要写足——每条四五百字起步，像一幕完整的戏：场景铺陈、人物动作神态、对白往来、内心活动逐层展开，三五句话就收等于没写；格式与正文同一套标记：「」包对白、*…*包角色心声、【】起场景行（开头可用一行标时间地点与在场人物）、~…~强调，不要写成无标记的白文；
- 示例对话是文风锚点不是剧情：对白凝练，答非所问处见性格。

▍画布制作规格：
开场画布是点进卡片时铺在深色封面图上的门面页，只在界面展示、不进提示词。
- 硬性规格（必须遵守）：画布默认 background: transparent，浅色元素浮在深色封面上；若你的设计概念需要整幅底色或氛围底（如深蓝晒图、雾面水墨），也可以自己画满，但必须与深色界面协调。容器 max-width 440px 居中、overflow-x hidden、高度随内容自然结束；绝对禁止给任何元素写 height:100vh、min-height:100vh、height:100% 这类视口/撑满写法——画布跑在自动量高的 iframe 里，视口单位会让内容高度反过来跟着 iframe 涨，形成越量越高的回路，页面被拖出一大截空白；想要"第一屏"的感觉就给版头一个固定像素高度（如 560px）。字体注意：沙盒 iframe 受浏览器字体反指纹限制，只看得到标准字体白名单——Songti SC/STSong 这类系统中文衬线在画布里查不到、会静默落回无衬线（实测），想要衬线或特色字体必须外链网络字体（如谷歌字体的 <link> 或 @font-face），系统字体栈只作加载失败时的兜底，版面不能因字体缺席而垮；整页字体在画布根容器上自己声明，绝不要写 font-family:inherit——宿主界面的字体设置够不到 iframe，inherit 只会继承到默认无衬线体；图片禁止编造外链（编出来的 URL 必然裂图），经图床上传得到的真实 URL 可以用，其余照片一律做成可替换占位框（低透明度底 + 虚线描边 + 小字标注，并在代码注释标明「把此处 url('') 换成图片」），作者之后自己贴图。
- 概念先行：先从这个角色的题材与气质里提炼一句话的设计概念（比如"停摆的怀表""收银小票的背面""雨夜档案袋""折花人的通缉令"），整页的配色、字体、模块划分都围绕它展开。禁止套固定模板——页面讲什么、分几个模块、每个模块叫什么，全由你按角色决定：档案、手札、时间线、诗句、信物、票据、社交账号快照、聊天记录、病历、任何"世界内的物件"都可以；模块名要像作品的一部分（如「停滞的秒针」），不要写"模块一/基础信息"这种工程名。
- 内容构思：先摆正体量——开场画布不是"一张卡片"，而是一整页纵向滚动的门面长页，读者是一路往下滚着读完的。模块通常 4~5 个即可，体量不靠堆模块数，靠把每个模块做深做厚：模块内部可用折叠栏、标签页、点击切换这类交互收纳内容——例如关系介绍做成几张人物小图，点击谁就展开谁的资料；经历介绍分多个时间段、每段一小段话逐段收纳。常见的组件有——版头、人物介绍卡、经历介绍（可配图）、关系介绍（他身边的人，可配图）、社交平台（聊天记录、公开主页等快照）、创意模块（展示角色的创意内容，不限形式）、音乐播放器。这些只是常见选项，不固定、不套路：选哪些、叫什么、什么顺序，都从角色设定出发取舍。组件之间留足留白；每个组件的内容要写满写实——关系介绍每人有立场与来路，不要一行带过；全页正文文字量普遍在几百字以上，把角色资料改写、扩充进各模块，不要只搬三五句话撑场面。
- 质量原则（风格千变，这几条不能破）：
  · 视觉层级明确：一眼看到的、第二眼看到的、凑近才看到的，用字号与透明度拉开；
  · 配色克制：一页一个主意，颜色为气质服务——水墨青绿、黑白猩红、深蓝晒图、鎏金古卷都可以，但不要多种鲜色打架、不要默认蓝紫渐变、不要 emoji 当图标；
  · 字体有对话：中文标题用衬线或书法感排法（可竖排、可逐字错落），英文小注可用等宽体拉开档案感；正文小字号、宽行高，颜色比标题收敛一档，但无论底色深浅，正文与底之间必须保住足够的明度差——氛围可以淡，关键信息不能看不清；
  · 留白是版面的一部分：密处密、疏处疏，不要均匀铺满。
- 可取用的美学技法（以下只是一些可能的技法，不是清单也不必凑全：按设计概念挑三五样组合即可，欢迎自创）：
  · 版面与图文：半透明卡片浮层配细描边；文字压图处垫局部渐变蒙版；图文并置像杂志页（竖幅图与文字栏并排、大小图对比）；多张竖图微倾斜扇排；照片配错位细描边空框、或垫一块偏移的实色色块；胶带贴照片（白边+微倾斜）；大图卡与内容区叠压错位；关系类条目左右交错（zigzag）；窄竖条画廊并排收纳多人（配竖排小字名）。
  · 排印：超大标题拉宽字距；相邻元素字号剧烈对比；竖排；空心描边幽灵字或巨型字母压底；词内字号颜色逐字错落；低透明度英文斜体叠印在中文标题上；两端对齐窄栏（栏内个别词换强调色）；边缘裁切大字只露局部当纹理；阶梯式短句逐行缩进；带引号短句左右交错如对话；展示性花体/手写体英文；正文关键词少量加粗或变色；句尾打字机光标。
  · 结构装饰：照片四角括号取景框；左竖线引用块；细横线+小灰标题分节；短横线夹注小节标（— 01 —）；编号斜杠小节标（01. ○○ //）；描边方框独立卡；胶囊标签组、标签格阵（其中一格反白作重点）、斜杠标签串、标签直接叠在照片角上；键值行+发丝线分隔；小数据格组（上小标签下大数值）；反白小标块全页最多一两处；标签页导航行（当前项下划线高亮）；双栏正文；圆点串；虚线空框；图标装饰分隔行（波形线、小符号串）；[展开][+] 类伪交互动作词缀框角；悬浮小圆钮。
  · 色彩与线条：一页一个主色调、与封面气质协调；双色极简（黑白灰+单一高饱和强调色，只给关键词与细线小点）；整页单色罩染做档案纸感；一条细线从版头蜿蜒到版尾串联全页；细网格/大圆弧线低透明度压底；单色线稿图形压底（与幽灵字同层级）。
  · 拟物形制（形制是公共语言，内容一律按角色原创）：社交页头（圆头像+名字+@句柄+签名）；取景器 HUD（REC 圆点、时间码、参数小字排四角）；档案页（卷号角标、条码、等宽字装饰性数据小字散角落）；纯排印播放器（播放符号+曲名+巨型幽灵字母，或 ⏮⏸⏭ 细线控件行）；波形/心电小图形块。
- 无论有没有 JS，内容必须完整可读：默认展开第一项，不准把内容藏死。

（若我在想法里注明「要 JSON」，请改为只输出一个可导入的 JSON 对象：从 { 开始、到 } 结束，不要用 \`\`\` 代码块包裹、前后不加任何文字；字符串值内不允许出现真实换行或制表符（会直接导致导入失败），所有换行一律写成 \\n，长代码字段尤其要逐行检查；请用分行缩进的格式输出整个 JSON（不要压成一行，压成一行极易漏括号）；代码或正则里的一个反斜杠在 JSON 里写成两个（如 JS 的 \\d 写成 \\\\d），不要再多转一层：{"kind":"character","name":"角色名","charName":"角色名","hook":"…","tags":["…"],"baseInfo":"…","personality":"…","appearance":"…","background":"…","worldview":"…","cognition":"…","relations":"…","plot":"…","extra":"…","openings":["开场白一","开场白二"],"examples":[{"role":"user","text":"…"},{"role":"char","text":"…"}],"canvas":"…"}，openings 至少一条。）

【我的想法】：（在这里描述你想要的角色）`,

    persona: `请帮我写一份 AI 角色扮演用的"用户人设"：描述玩家扮演的 {{user}} 是谁。只写玩家这一侧——身份、性格、外貌、经历、说话习惯；不要写对手角色，也不要替剧情做决定。正文里可用 {{user}} 指代本人、{{char}} 指代对手角色。

请分段输出：
【材料名】给这份人设起个名，方便在酒柜里认出来
【一句话介绍】一行说清这是个什么人
【标签】2~6 个短词，用顿号隔开
【你的名字】一个合适的名字
【用户人设】人设正文

（若我在想法里注明「要 JSON」，请改为只输出（从 { 开始、到 } 结束，不要用 \`\`\` 代码块包裹、前后不加任何文字；字符串值内不允许出现真实换行或制表符（会直接导致导入失败），所有换行一律写成 \\n，长代码字段尤其要逐行检查；请用分行缩进的格式输出整个 JSON（不要压成一行，压成一行极易漏括号）；代码或正则里的一个反斜杠在 JSON 里写成两个，不要再多转一层）：{"kind":"persona","name":"材料名","hook":"…","tags":["…"],"userName":"名字","content":"人设正文"}，纯 JSON、字符串内换行写成 \\n。）

【我的想法】：（写你想扮演什么样的人）`,

    base: `请帮我写一份 AI 角色扮演的"扮演总纲"：只约束 AI 的扮演态度，不涉及文笔。典型条目：完全成为{{char}}以第一视角活在故事里；绝不跳出角色、不以 AI 自称；绝不代替{{user}}说话或做决定；允许冲突、拒绝与负面情绪，贴合人设比讨好{{user}}更重要。

请分段输出：
【材料名】给这份总纲起个名
【一句话介绍】一行说清它的取向
【标签】2~6 个短词，用顿号隔开
【扮演总纲】结合我的想法给 5~8 条这样的规则（可用 ### 加小标题），不要另加解释

（若我在想法里注明「要 JSON」，请改为只输出（从 { 开始、到 } 结束，不要用 \`\`\` 代码块包裹、前后不加任何文字；字符串值内不允许出现真实换行或制表符（会直接导致导入失败），所有换行一律写成 \\n，长代码字段尤其要逐行检查；请用分行缩进的格式输出整个 JSON（不要压成一行，压成一行极易漏括号）；代码或正则里的一个反斜杠在 JSON 里写成两个，不要再多转一层）：{"kind":"base","name":"材料名","hook":"…","tags":["…"],"content":"正文"}，纯 JSON、字符串内换行写成 \\n。）

【我的想法】：（想要的基调或特殊的扮演边界，例如更黑暗、更治愈、绝不快进剧情）`,

    flavor: `请帮我写一段 AI 角色扮演的"文风要求"：只约束怎么写，不承载任何角色设定。可以约束：句式长短、叙述视角与人称、动作/心理/环境描写的详略比例、对白密度与留白、修辞习惯与忌讳（如禁用的陈词滥调）。

请分段输出：
【材料名】给这份文风起个名
【一句话介绍】一行说清它的味道
【标签】2~6 个短词，用顿号隔开
【文风】可直接使用的文风指令，条目式或短段落均可，不要另加解释

（若我在想法里注明「要 JSON」，请改为只输出（从 { 开始、到 } 结束，不要用 \`\`\` 代码块包裹、前后不加任何文字；字符串值内不允许出现真实换行或制表符（会直接导致导入失败），所有换行一律写成 \\n，长代码字段尤其要逐行检查；请用分行缩进的格式输出整个 JSON（不要压成一行，压成一行极易漏括号）；代码或正则里的一个反斜杠在 JSON 里写成两个，不要再多转一层）：{"kind":"flavor","name":"材料名","hook":"…","tags":["…"],"content":"正文"}，纯 JSON、字符串内换行写成 \\n。）

【我的想法】：（想要什么味道的文字；可以贴一小段你喜欢的例文让我模仿）`,

    glass: `请帮我写一段 AI 角色扮演的"正文输出要求"：约束每轮回复的形态——段落数量、叙述节奏、收笔方式（例如：每轮 2~4 个自然段；叙述里穿插动作与环境细节；在留有余韵处收笔，给{{user}}接话空间）。

注意：正文标记规则（「」对白、*…*心声、【】场景行、~…~强调）由应用内置，不要重复写。

请分段输出：
【材料名】给这份输出要求起个名
【一句话介绍】一行说清它的形态
【标签】2~6 个短词，用顿号隔开
【正文输出要求】可直接使用的要求正文

（若我在想法里注明「要 JSON」，请改为只输出（从 { 开始、到 } 结束，不要用 \`\`\` 代码块包裹、前后不加任何文字；字符串值内不允许出现真实换行或制表符（会直接导致导入失败），所有换行一律写成 \\n，长代码字段尤其要逐行检查；请用分行缩进的格式输出整个 JSON（不要压成一行，压成一行极易漏括号）；代码或正则里的一个反斜杠在 JSON 里写成两个，不要再多转一层）：{"kind":"glass","name":"材料名","hook":"…","tags":["…"],"content":"正文"}，纯 JSON、字符串内换行写成 \\n。）

【我的想法】：（想要多长的回复、什么节奏）`,

    strength: `请帮我提炼 AI 角色扮演的"最高优先级要求"：这段话会被放在离生成最近的位置，模型对它服从度最高，但条目越多互相稀释——只要一到两条最需要被死守的规则，每条一句话。

请分段输出：
【材料名】给它起个名
【一句话介绍】一行说清它守什么
【标签】2~4 个短词，用顿号隔开
【最高优先级要求】一到两条，直接可用，不要解释

（若我在想法里注明「要 JSON」，请改为只输出（从 { 开始、到 } 结束，不要用 \`\`\` 代码块包裹、前后不加任何文字；字符串值内不允许出现真实换行或制表符（会直接导致导入失败），所有换行一律写成 \\n，长代码字段尤其要逐行检查；请用分行缩进的格式输出整个 JSON（不要压成一行，压成一行极易漏括号）；代码或正则里的一个反斜杠在 JSON 里写成两个，不要再多转一层）：{"kind":"strength","name":"材料名","hook":"…","tags":["…"],"content":"正文"}，纯 JSON、字符串内换行写成 \\n。）

【我的想法】：（最不能容忍模型犯什么毛病）`,

    ticket: `请帮我制作 AI 角色扮演对局里的"状态栏"。

它是什么：每轮 AI 回复的开头都会附一张小卡片，把看不见的剧情状态变成看得见的。内容设计空间很大（以下都只是参考）：可以是数值与事实——好感度/关系阶段、时间地点天气、金钱物品、任务进度、条件才显形的隐藏项；可以是描述性的——角色此刻的动作神态、衣着、内心状态、生理反应；可以带戏——好感变化标注原因（进度 2%，±0，原因：荒山遇绝色）、角色的内心 OS 与吐槽小字；还可以挂扩展小节——群聊消息、论坛或江湖八卦、来信、记忆卷轴、黄历宜忌。它由两半组成：

① 输出契约：写给扮演 AI 的指令——每轮在状态栏壳内用固定的键值行格式报告状态（例如「好感度：62」一行一项）。壳标记（[状态栏]…[/状态栏]，一局挂多张状态栏时开标签还会带上各张的名字）由应用统一向模型说明，契约只写内容与格式要求。设计要点：状态项分组归入小节，核心数值放最前；数值项给范围与每轮变化规则，文字项给可取值枚举或写法要求（如「内心 OS：一句第一人称，20~40 字」）；每个小节的格式在契约里定死（固定的小节标题行），渲染代码才解析得动；键名与小节名保持稳定，不要每轮换词。信息量要在契约里逼出来：整卡通常 6~10 项，描述类的值必须在契约里要求写成带细节的完整句——外貌写到具体衣物与磨损（「腰悬无鞘长刀，刀柄缠布磨得发白」的密度）、动作写到小动作、心绪带一句原因，不许两三个词敷衍；再安排一两个长文小节（内心独白、江湖传闻、日志、待办清单之类，两三句成段），数值项可带括号注释或变化原因；这样每轮状态栏正文有一两百字的血肉——若契约只要三五行「好感度/时间/地点」，等于没做。若我的想法很简略，就按题材替我定夺：选最能承载这类故事张力的状态项，并在契约里写清变化逻辑。

② 渲染代码：一份完整 HTML（可含 CSS/JS），把壳内原文渲染成卡片。
- 注入方式（只有这两个名字，不要发明别的宏）：{{RAW}} 是纯文本替换，只能放在 HTML 标签内容里当排版占位，绝不能写进 JS 代码（替换后会破坏语法）；JS 里一律用 window.TICKET_RAW 取原文字符串，兜底写法 var raw = window.TICKET_RAW || '示例\\n数据'；window.MIX_STATE 是被「记住」的值组成的对象。注意：注入的就是壳内原文本身、不含 [状态栏] 标记，解析时不要去找壳标记；JS 里如需内置示例文本，字符串必须用 \\n 转义换行，绝不能把字符串字面量写成多行（会直接语法错误）。解析要容错：某项缺失时显示占位符，不能整卡崩掉。
- 运行环境：沙盒 iframe（权限隔离、碰不到应用数据）。字体注意：沙盒里系统字体只剩标准白名单，Songti SC 这类系统中文衬线查不到、会静默落回无衬线——要衬线或特色字体必须外链网络字体（如谷歌字体），系统字体栈只作兜底；图片禁止编造外链，经图床上传得到的真实 URL 可以用。高度由内容自然撑开（应用会量高自适应），禁止给 body 或任何容器写 height:100vh、min-height:100vh、height:100% 这类撑满写法。
- 美学要求：概念先行——先从题材气质里定这张卡"是什么"，风格光谱很宽（以下都只是参考）：世界内的物件（便利店小票、玉牌灵力盘、老黄历、任务终端）、玻璃拟态的轻 UI 面板、红黑武侠式的杂志排印、终端与档案流水、诗行与卷轴……选一种贴角色的做透，不要千篇一律的圆角渐变仪表盘。布局纪律：它每轮都插在对话流里——核心状态第一眼可见；内容多就分模块折叠收纳（默认展开核心、次要收起），不必硬塞成一小块；信息层级分明、配色一页一个主意且与深色聊天界面协调；动效轻，不要每轮都闪。

请分段输出：
【材料名】给这张状态栏起个名
【一句话介绍】一行说清它跟踪什么、什么风格
【标签】2~6 个短词，用顿号隔开
【输出契约】
【渲染代码】完整 HTML
【预览示例数据】壳内原文本身：只有契约定义的那些键值行，不要带 [状态栏] 标记、不要带正文、不要用代码块包裹
【建议记住的变量】需要跨轮留存、供生效条件判断的键名及初始值；直接写键名，不要用反引号或引号包裹

（若我在想法里注明「要 JSON」，请改为只输出（从 { 开始、到 } 结束，不要用 \`\`\` 代码块包裹、前后不加任何文字；字符串值内不允许出现真实换行或制表符（会直接导致导入失败），所有换行一律写成 \\n，长代码字段尤其要逐行检查；请用分行缩进的格式输出整个 JSON（不要压成一行，压成一行极易漏括号）；代码或正则里的一个反斜杠在 JSON 里写成两个，不要再多转一层）：{"kind":"ticket","name":"材料名","hook":"…","tags":["…"],"contract":"…","renderHtml":"…","previewRaw":"…","vars":[{"name":"好感度","initial":"50"}]}，纯 JSON、字符串内换行写成 \\n。）

【我的想法】：（想跟踪哪些状态、卡片什么风格）`,

    garnish: `请帮我写一份 AI 角色扮演对局界面的美化 CSS（只能是 CSS，会被限定在对局画面内生效）。整个对局画面都归你管，可用的官方类名分两组：

正文语义类：.mix-prose 正文容器 / .mix-para 普通段落（默认首行缩进 2em）/ .mix-scene 场景过场行 / .mix-dialogue 对白 / .mix-thought 心声 / .mix-accent 强调 / .mix-narration 叙述

界面部件类：.mix-game 对局画面根（写 body、html、:root 也等同于它）/ .mix-game-bg 封面背景层 / .mix-game-header 顶部标题栏 / .mix-game-title 标题文字 / .mix-icon-btn 图标按钮（标题栏与输入栏两侧）/ .mix-game-scroll 对话滚动区 / .mix-user-turn 玩家轮容器 / .mix-user-bubble 玩家气泡 / .mix-assistant-turn AI 轮容器 / .mix-turn-act 消息角落的复制回溯编辑小按钮 / .mix-game-inputbar 底部输入栏 / .mix-game-input 输入框 / .mix-send-btn 发送按钮 / .mix-state-bar 记住值状态条 / .mix-state-chip 状态小芯片 / .mix-ticket-wrap 状态栏卡片外框 / .mix-encore-inline 小剧场容器 / .mix-game-thinking 生成中指示

注意：标题栏可以换装但不能藏（返回按钮在里面，应用会强制它可见可点）；输入栏别挤出屏幕；字体可用 @font-face 引入外链字体（写好系统兜底栈；@import 会被清理器丢弃，不要用）；图片禁止编造外链，经图床上传得到的真实 URL 可以用；注意文字在深色界面上的可读性。一套好外观通常正文与部件一起做——气泡、输入栏、标题栏与正文同一个气质。

请分段输出：
【材料名】给这套外观起个名
【一句话介绍】一行说清它的气质
【标签】2~6 个短词，用顿号隔开
【外观 CSS】一份完整 CSS，不要解释

（若我在想法里注明「要 JSON」，请改为只输出（从 { 开始、到 } 结束，不要用 \`\`\` 代码块包裹、前后不加任何文字；字符串值内不允许出现真实换行或制表符（会直接导致导入失败），所有换行一律写成 \\n，长代码字段尤其要逐行检查；请用分行缩进的格式输出整个 JSON（不要压成一行，压成一行极易漏括号）；代码或正则里的一个反斜杠在 JSON 里写成两个，不要再多转一层）：{"kind":"garnish","name":"材料名","hook":"…","tags":["…"],"css":"…"}，纯 JSON、字符串内换行写成 \\n。）

【我的想法】：（想要的配色与气质，例如宣纸古风、霓虹赛博、便利店收银小票）`,

    encore: `请帮我制作 AI 角色扮演对局的"小剧场"。

它是什么：正文之外的"加演"，渲染成一小块画面跟在回复后面，给故事开一扇侧窗。典型用法（供参考）：角色的朋友圈/微博动态（带点赞与评论）、监控或直播画面、路人视角的短评、日记或信纸的一页、新闻推送、深夜电台、系统公告……分两种：AI 供稿型（写契约，AI 每轮按契约供稿）与纯静态小品（不写契约，画面固定）。它由两半组成：

① 输出契约（可选）：写给扮演 AI 的指令——壳内按什么格式写。小剧场默认每轮都演：像状态栏一样，每轮回复后面都跟一块，契约把每轮的内容格式定死（明确的行结构，方便渲染代码解析）；只有当设计本身是节点型的（如事件快讯、整点报时）才在契约里写触发条件，不满足时整段不输出。每轮都要演足——小剧场是玩家等生成时读的加餐，好的小剧场有梗、有结构，常常比正文还耐看，要按一篇微型作品的量来要求：社交帖是完整一楼——配图以文字描述画面（渲染代码可做成「点击查看」式图卡）、正文文案、点赞数、再盖一串评论楼，评论各有人名与口吻、有来有回可以歪楼拌嘴；情景短剧是完整一幕——几拍分镜有铺垫有反转、笑点或刀点收尾，可配一张道具卡（转账单、车票、化验单）；监控、电台、新闻同理，都要成篇成件。把这个量写进契约当每轮的硬要求（如「评论至少 5 条、各不同人、其中一条盖楼回复」），每轮通常十来行、两三百字起步——三五行的加演等于没演。纯静态小品同理：画面做足一件完整物件（一整页日记、一张写满的票据），不要一行字撑一块画面。壳标记（[小剧场]…[/小剧场]）与"不满足条件就不输出"由应用统一向模型说明，契约只写时机与内容格式；不要发明占位协议（比如"没有内容时输出空 JSON / noop"），不满足条件时什么都不输出就是正确行为。若做纯静态小品，契约留空。

② 渲染代码：完整 HTML（可含 CSS/JS）。
- 注入方式（只有这两个名字，不要发明别的宏）：{{RAW}} 是纯文本替换，只能放在 HTML 标签内容里当排版占位，绝不能写进 JS 代码（替换后会破坏语法）；JS 里一律用 window.ENCORE_RAW 取原文字符串，兜底写法 var raw = window.ENCORE_RAW || '示例\\n数据'；window.MIX_STATE 是被记住的值。注意：注入的就是壳内原文本身、不含 [小剧场] 标记；JS 里如需内置示例文本，字符串必须用 \\n 转义换行，绝不能写成多行字面量。解析要容错，内容缺项不崩。
- 运行环境：沙盒 iframe（权限隔离、碰不到应用数据）。字体注意：沙盒里系统字体只剩标准白名单，Songti SC 这类系统中文衬线查不到、会静默落回无衬线——要衬线或特色字体必须外链网络字体（如谷歌字体），系统字体栈只作兜底；图片禁止编造外链，经图床上传得到的真实 URL 可以用。高度由内容自然撑开，禁止给 body 或任何容器写 height:100vh、min-height:100vh、height:100% 这类撑满写法。
- 美学要求：概念先行，风格不限一种路数：拟物是常见做法（朋友圈像真的社交 App 卡片、监控带时间码与噪点、日记有纸感），纯排印同样成立——一页竖排诗行、一角剪报、一段终端流水、一折戏文。选贴题材的做透；质感要与正文区分开，让人一眼知道"这是加演"；配色克制、与深色界面协调。

请分段输出：
【材料名】给这出小剧场起个名
【一句话介绍】一行说清它演什么
【标签】2~6 个短词，用顿号隔开
【输出契约】（纯静态小品则写"无"）
【渲染代码】完整 HTML
【预览示例数据】壳内原文本身：只有契约定义的内容，不要带 [小剧场] 标记、不要带正文、不要用代码块包裹（纯静态则写"无"）

（若我在想法里注明「要 JSON」，请改为只输出（从 { 开始、到 } 结束，不要用 \`\`\` 代码块包裹、前后不加任何文字；字符串值内不允许出现真实换行或制表符（会直接导致导入失败），所有换行一律写成 \\n，长代码字段尤其要逐行检查；请用分行缩进的格式输出整个 JSON（不要压成一行，压成一行极易漏括号）；代码或正则里的一个反斜杠在 JSON 里写成两个，不要再多转一层）：{"kind":"encore","name":"材料名","hook":"…","tags":["…"],"contract":"…","renderHtml":"…","previewRaw":"…"}，纯 JSON、字符串内换行写成 \\n。）

【我的想法】：（想加演什么、长什么样、什么时候出现）`,

    filter: `请帮我编写一组"正文清洗规则"，用于自动清理 AI 回复里的文字怪癖。每条规则包含三样：
- 查找：JS 正则（自动加 g 标志，不要写斜杠定界符），可用捕获组；
- 替换：替换文本（$1 引用捕获组；留空即删除）；
- 模式：「仅显示」= 只在渲染时替换，对全部历史即时生效、可随时反悔；「进上下文」= 入库前清洗，发回模型的历史也是洗过的，只影响新回复。

请分段输出：
【材料名】给这张滤网起个名
【一句话介绍】一行说清它洗什么
【标签】2~4 个短词，用顿号隔开
【清洗规则】每条一行，按「查找 → 替换 → 模式 → 一句话说明」排列；查找与替换直接给字符本体，不要用反引号、引号或斜杠包裹；某条的替换是删除时写「（删除）」两个字，不要写（空）之类容易被照抄的占位；注意正则别误伤正常标点

（若我在想法里注明「要 JSON」，请改为只输出（从 { 开始、到 } 结束，不要用 \`\`\` 代码块包裹、前后不加任何文字；字符串值内不允许出现真实换行或制表符（会直接导致导入失败），所有换行一律写成 \\n，长代码字段尤其要逐行检查；请用分行缩进的格式输出整个 JSON（不要压成一行，压成一行极易漏括号）；代码或正则里的一个反斜杠在 JSON 里写成两个，不要再多转一层）：{"kind":"filter","name":"材料名","hook":"…","tags":["…"],"rules":[{"find":"\\\\*\\\\*","replace":"","mode":"display"}]}，mode 取 display 或 context，纯 JSON。）

【我的想法】：（想清洗什么，例如满屏的省略号、破折号、加粗星号）`,

    mechanism: `请帮我编写 AI 角色扮演对局用的"机括"——给对局装上一件可执行的玩法或工具。

它是什么：机括分两半，可以只做一半；两半共用同一份存储（store），天然联动。
- 钩子逻辑：一段 JS，应用在每轮的固定时机自动调用它，能读写状态、改写文本、给模型塞只生效一轮的临时提示；
- 常驻界面：一块挂在对局画面上的 HTML 面板，跨轮存在，玩家可以点按。
典型玩法（供参考，可组合可自创）：
· 骰子/抽卡/随机事件面板：界面按钮掷骰或抽签，用 mix.say 以玩家身份把结果说进对局；
· 数值仪表：读 MIX_STATE 画好感度进度条、金钱背包面板，数值由状态栏的「记住」项供给；
· 节奏器：钩子数轮数，到第 N 轮用 note 给模型塞一条临时提示（推进剧情、触发随机事件）；
· 文本加工：onBeforeSend 改写玩家的话（如替换称呼、附加动作格式），onAfterReply 加工模型正文；
· 记事本：钩子每轮把剧情要点摘进 store，界面做成可翻页的日记/档案面板；
· 世界钟：维护游戏内时间与日程，把当前时辰写进 note 让模型遵守。
若我的想法说得笼统：先在【玩法方案】里用一两句话复述你理解的玩法，把它拆成「钩子做什么 / 界面做什么 / 数据存哪」；拿不准就先做最小可用版本，最后列两三个可选扩展。

① 钩子逻辑（纯 JS，可选）：定义这些可选的全局函数，应用会在对应时机调用：
function onSessionStart(ctx) / onBeforeSend(ctx) / onAfterReply(ctx) / onSessionEnd(ctx)
ctx 字段：turnCount 已发生轮数；state 记住的值；store 本机括自己的存储；charName / userName；text（发送前 = 玩家这句话，回复后 = 模型正文）；ticketRaw / encoreRaw（回复后的状态栏与小剧场原文）。
返回一个普通对象（各项都可省略）：{ text: 改写后的 text, note: 只在这一轮生效的临时提示（≤2000字）, state: 要写入的记住值, store: 覆盖自己的存储 }。
限制：单次执行 2 秒超时；无网络、碰不到页面；存储上限 100KB。

② 常驻界面（完整 HTML，可选）：跑在沙盒 iframe 里。用 window.MIX_STATE / window.MIX_STORE 读数据，定义 window.onMixSync(state, store) 接收更新；通过 window.mix 请求动作：setStore(obj)、setState(obj)、say(text) 以玩家身份发言、move(x,y) 与 size(w,h)（占对局画面的百分比）、fit(px) 报内容高度、design(px) 设排版基准宽度、drag(bool)/resize(bool)/chrome(bool)/plate(bool)、z(n)、grab() 在自绘标题条上起拖。界面初始无外壳无底板，位置与尺寸请在代码里用 mix.move / mix.size 自己定好。除自由悬浮外还有五个挂点（材料的 layout.slot 字段声明，代码里不可改）："header"/"inputbar-left"/"inputbar-right" 三个按钮位——宿主在标题栏或输入栏画一颗图标按钮（图标由 layout.icon 给一两个 emoji），点击开合面板，面板宽度铺满、高度随内容，适合骰子/道具/快捷指令这类召之即来的工具；"flow-top"/"flow-bottom" 两个流内位——面板作为内嵌卡进滚动流（画布之下/最新一轮之下），随内容滚动，适合任务看板、选择器这类跟着剧情走的界面。非悬浮挂点下 move/size/drag/resize/chrome 无效，fit/design/plate 照常；按钮位面板关闭时会被卸载，要留住的状态写进 store。界面里可用的数据只有 MIX_STATE 与 MIX_STORE 两个对象——没有角色名、玩家名这类现成变量，需要就让钩子写进 store 再读；写完自查一遍：用到的每个变量都必须已声明。
- 美学要求：形态跟着玩法走——一颗胶囊、一条窄边栏、一枚角标、一张卡片，或一块整面的仪表盘都可以。原则只有三条：默认别挡住对话（大面板要能收起或退场）；气质贴合对局，把它当世界里的道具来做（罗盘、签筒、终端、账本），不要做成工程感的调试面板；动效轻、不抢注意力。配色与深色界面协调。

请分段输出：
【材料名】给这件机括起个名
【一句话介绍】一行说清它是什么玩法
【标签】2~6 个短词，用顿号隔开
【玩法方案】一两句话：这件机括怎么玩，钩子和界面各负责什么
【钩子逻辑】（不需要则写"无"）
【界面代码】完整 HTML（不需要则写"无"）

（若我在想法里注明「要 JSON」，请改为只输出（从 { 开始、到 } 结束，不要用 \`\`\` 代码块包裹、前后不加任何文字；字符串值内不允许出现真实换行或制表符（会直接导致导入失败），所有换行一律写成 \\n，长代码字段尤其要逐行检查；请用分行缩进的格式输出整个 JSON（不要压成一行，压成一行极易漏括号）；代码或正则里的一个反斜杠在 JSON 里写成两个，不要再多转一层）：{"kind":"mechanism","name":"材料名","hook":"…","tags":["…"],"script":"…","panelHtml":"…"}，纯 JSON、字符串内换行写成 \\n。）

【我的想法】：（想实现什么玩法，例如骰子面板、好感度进度条、随机事件抽卡）`,
};

// ── 小卷工具版规格 ─────────────────────────────────────
// 桌面助手小卷通过工具直写酒柜，不走"复制发给外部 AI"的流程——
// 分段输出清单、JSON 口令、【我的想法】这些外发专用段对它没有意义，
// 字段以工具参数直接传。这里从同一份委托词派生出工具版规格：
// 机制与质量约束一字不差地共享，外发格式段剥掉，再补一份字段对照。
// 改约束只改上面的委托词，两边同时生效。

/** 每类材料的工具字段对照（工具版规格专用，委托词不含） */
const MIX_CRAFT_FIELD_NOTES: Record<MixMaterialKind, string> = {
    character: `—— 工具字段对照 ——
name＝角色名（同时是卡名）；hook＝一句话介绍；tags＝字符串数组（3~8 个短词）；
baseInfo/personality/appearance/background/worldview/cognition/relations/plot/extra 对应基础信息/性格/外貌/背景/世界观/初始认知/关系与身份/当前剧情/附加设定；
openings＝字符串数组，每个元素是一条完整开场白，至少两条供玩家挑选（不需要 --- 分隔符）；
examples＝数组 [{"role":"user"|"char","text":"…"}]，至少 2 轮（4 条），3~5 轮更好；canvas＝开场画布完整 HTML 字符串；cover＝封面图地址（https URL 或 dataURL，可用图床上传取得）。
质量要求：开场白写满 2~3 条、每条四五百字起步且用正文标记书写（「」对白、*…*心声、【】场景行、~…~强调），示例对话写满 3~5 轮；画布按上方规格完整制作，不要缩水成一张简单信息卡交差。
图片细则：画布里禁止编造图片外链；经图床上传得到的真实 URL 可以用，没有图床就留可替换占位框。`,
    persona: `—— 工具字段对照 ——
name＝材料名；hook＝一句话介绍；tags＝字符串数组；userName＝你的名字；content＝人设正文。`,
    base: `—— 工具字段对照 ——
name＝材料名；hook＝一句话介绍；tags＝字符串数组；content＝扮演总纲正文。`,
    flavor: `—— 工具字段对照 ——
name＝材料名；hook＝一句话介绍；tags＝字符串数组；content＝文风正文。`,
    glass: `—— 工具字段对照 ——
name＝材料名；hook＝一句话介绍；tags＝字符串数组；content＝正文输出要求。`,
    strength: `—— 工具字段对照 ——
name＝材料名；hook＝一句话介绍；tags＝字符串数组；content＝最高优先级要求（一到两条）。`,
    ticket: `—— 工具字段对照 ——
name＝材料名；hook＝一句话介绍；tags＝字符串数组；contract＝输出契约；renderHtml＝渲染代码完整 HTML；
previewRaw＝预览示例数据：壳内原文本身，只有契约定义的内容，不带 [状态栏] 标记、不带正文；
vars＝要跨轮记住的变量，数组 [{"name":"好感度","initial":"50"}]，键名不带引号反引号；
historyFeed＝历史回传（选填）："latest"（默认）只把最近一轮的状态栏原文回传给模型——状态接续与格式示范都够用，长局不费 token；"all" 全部轮次回传，仅当契约需要引用往期状态栏内容时才用；"none" 完全不回传，纯展示型状态栏最省 token。
列表封面由渲染效果自动生成（渲染代码 + 预览示例数据），不收 cover。
图片细则：渲染代码里禁止编造图片外链；经图床上传得到的真实 URL 可以用。`,
    garnish: `—— 工具字段对照 ——
name＝材料名；hook＝一句话介绍；tags＝字符串数组；css＝完整 CSS。`,
    encore: `—— 工具字段对照 ——
name＝材料名；hook＝一句话介绍；tags＝字符串数组；contract＝输出契约（纯静态小品不传）；
renderHtml＝渲染代码完整 HTML；previewRaw＝壳内原文本身，不带 [小剧场] 标记、不带正文（纯静态不传）；
historyFeed＝历史回传（选填）："latest"（默认）只把最近一轮的小剧场原文回传给模型——格式示范够用，长局不费 token；"all" 全部轮次回传，仅当契约需要衔接往期小剧场剧情时才用；"none" 完全不回传，每出独立、不需要接续时最省 token。
列表封面由渲染效果自动生成，不收 cover。
图片细则：渲染代码里禁止编造图片外链；经图床上传得到的真实 URL 可以用。`,
    filter: `—— 工具字段对照 ——
name＝材料名；hook＝一句话介绍；tags＝字符串数组；
rules＝数组 [{"find":"正则本体（不带斜杠定界符）","replace":"替换文本（删除传空串）","mode":"display"|"context"}]。`,
    mechanism: `—— 工具字段对照 ——
name＝材料名；hook＝一句话介绍；tags＝字符串数组；script＝钩子逻辑纯 JS（可不传）；
panelHtml＝常驻界面完整 HTML（可不传）；script 与 panelHtml 至少传一个；
layout＝摆放对象（选填）：{"slot":"float|header|inputbar-left|inputbar-right|flow-top|flow-bottom","icon":"🎲","x":3,"y":62,"w":94,"h":20,"autoHeight":true,…}——slot 不写为自由悬浮；按钮位（header/inputbar-*）配 icon 一两个 emoji，宿主画按钮点击开合；流内位（flow-top/flow-bottom）嵌进滚动流随内容滚动。
图片细则：机括沙盒完全断网（CSP default-src 'none'），任何外链（含图床 URL）都加载不了，界面素材只能内联。`,
};

/**
 * 工具版规格：委托词剥掉外发专用段（分段输出清单、JSON 口令、【我的想法】），
 * 保留机制与质量约束原文，末尾附字段对照。
 */
export function buildMixCraftSpec(kind: MixMaterialKind): string {
    let text = MIX_CRAFT_PROMPTS[kind];
    // ① 掐掉 JSON 口令段到结尾（含【我的想法】）
    const jsonAt = text.indexOf("（若我在想法里注明「要 JSON」");
    if (jsonAt >= 0) text = text.slice(0, jsonAt);
    // ② 掐掉分段输出块：从「请…输出：」标记行到其后的第一个空行
    const marker = text.match(/^请(按下面的框逐段|分段|分四段)输出[^\n]*$/m);
    if (marker && marker.index !== undefined) {
        const after = text.indexOf("\n\n", marker.index);
        text = after >= 0 ? text.slice(0, marker.index) + text.slice(after + 2) : text.slice(0, marker.index);
    }
    return `${text.trim()}\n\n${MIX_CRAFT_FIELD_NOTES[kind]}`;
}
